services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: profile-gen-backend
    env_file:
      - ./backend/.env
    environment:
      PORT: 3001
    ports:
      - "3001:3001"
    restart: unless-stopped

  memory-service:
    build:
      context: ./memory-service
      dockerfile: Dockerfile
    container_name: profile-gen-memory-service
    env_file:
      - ./memory-service/.env
    environment:
      PORT: 3002
    ports:
      - "3002:3002"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: profile-gen-frontend
    depends_on:
      - backend
    ports:
      - "4200:80"
    restart: unless-stopped
